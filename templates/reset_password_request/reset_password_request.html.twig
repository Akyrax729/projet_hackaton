{% extends 'base.html.twig' %}

{% block title %}Hello ResetPasswordRequestController!{% endblock %}

{% block main %}
    {{form_start(requestForm)}}
        {{form_widget(requestForm)}}
        <button>Envoyer</button>
    {{form_end(requestForm)}}
{% endblock %}
